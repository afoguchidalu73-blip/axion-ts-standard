[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "axion-ai"
version = "0.1.0"
description = "Causal enforcement gate for AI"
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
axion = "axion.cli:main"
